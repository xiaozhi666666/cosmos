# 区块链功能测试指南

## 🔄 完整的数据交互流程测试

这个指南将帮您验证钱包管理、代币生产、代币转账、挖矿和区块链浏览器之间的数据交互是否正确工作。

## 🚀 启动应用

```bash
npm install  # 如果还没有安装依赖
npm start    # 启动开发服务器
```

应用将在 `http://localhost:3000` 启动。

## 📝 测试步骤

### 第一步：钱包管理测试

1. **创建钱包**
   - 点击"创建新钱包"
   - 确认助记词被生成并显示
   - 验证钱包地址格式正确
   - 检查初始余额（应该为空或显示创世余额）

2. **导入钱包**  
   - 使用生成的助记词导入钱包
   - 验证地址匹配
   - 确认余额正确显示

3. **刷新余额功能**
   - 点击"刷新余额"按钮
   - 验证余额从区块链获取并更新

**预期结果**: 
- ✅ 钱包创建成功，地址格式为 `cosmos1...`
- ✅ 余额显示格式：`数量 代币名称`
- ✅ 刷新功能正常工作

### 第二步：代币铸造测试

1. **选择钱包**
   - 确保已选择一个钱包
   - 切换到"代币生产"标签页

2. **铸造代币**
   - 填写代币名称：`Test Token`
   - 填写代币符号：`TEST`  
   - 填写铸造数量：`1000`
   - 点击"铸造代币"

3. **验证铸造结果**
   - 等待交易提交到区块链
   - 检查铸造历史记录
   - 切换回钱包管理查看余额更新

**预期结果**:
- ✅ 代币铸造成功，获得交易哈希
- ✅ 铸造记录保存到历史
- ✅ 钱包余额增加相应的代币数量

### 第三步：代币转账测试

1. **准备转账**
   - 切换到"代币转账"标签页
   - 确认当前钱包显示正确

2. **创建第二个钱包**
   - 返回钱包管理，创建另一个钱包
   - 记录新钱包地址

3. **执行转账**
   - 选择第一个钱包
   - 输入第二个钱包地址作为接收方
   - 输入转账金额：`100`
   - 选择代币类型：`TEST`
   - 点击"发送转账"

4. **验证转账结果**  
   - 等待交易提交到交易池
   - 等待挖矿确认（可能需要手动挖矿）
   - 检查发送方和接收方余额变化

**预期结果**:
- ✅ 转账交易成功提交，获得交易哈希
- ✅ 发送方余额减少
- ✅ 接收方余额增加（需要等待区块确认）

### 第四步：挖矿测试

1. **开始挖矿**
   - 切换到"挖矿"标签页
   - 选择验证者
   - 点击"开始挖矿"

2. **观察挖矿过程**
   - 等待挖矿进度条完成
   - 查看挖矿奖励
   - 检查挖矿统计信息更新

3. **验证奖励发放**
   - 返回钱包管理
   - 检查钱包余额是否增加COSMOS代币
   - 验证挖矿历史记录

**预期结果**:
- ✅ 挖矿成功，获得区块奖励
- ✅ 钱包余额增加COSMOS代币
- ✅ 挖矿统计正确更新
- ✅ 挖矿历史记录保存

### 第五步：区块链浏览器测试

1. **查看最新区块**
   - 切换到"区块浏览器"标签页
   - 查看最新区块信息
   - 验证区块高度递增

2. **搜索特定区块**
   - 输入区块高度（如：1）
   - 点击搜索
   - 查看区块详细信息

3. **验证自动更新**
   - 进行一次挖矿操作
   - 观察区块浏览器是否自动更新
   - 检查最新区块包含的交易

**预期结果**:
- ✅ 区块信息正确显示
- ✅ 区块搜索功能正常
- ✅ 每10秒自动刷新
- ✅ 新挖出的区块立即显示

### 第六步：完整流程测试

**完整的用户旅程**:
1. 创建钱包A → 创建钱包B
2. 向钱包A铸造1000个TEST代币  
3. 从钱包A向钱包B转账500个TEST代币
4. 用钱包B进行挖矿，获得COSMOS奖励
5. 在区块浏览器中查看所有这些操作的记录

**验证要点**:
- ✅ 每个操作都生成真实的区块链交易
- ✅ 余额变化准确反映在区块链上
- ✅ 所有交易都可以在区块中查看
- ✅ 数据持久化，刷新页面后数据仍存在

## 🔍 调试和监控

### 浏览器开发者工具
```javascript
// 在控制台中检查区块链状态
cosmosService.getBlockchainStats()

// 查看当前钱包余额
cosmosService.getBalance('钱包地址')

// 查看最新区块
cosmosService.getLatestBlock()

// 查看交易池状态  
cosmosService.getTransactionPoolStatus()
```

### 本地存储检查
```javascript
// 检查钱包数据
JSON.parse(localStorage.getItem('cosmos-wallets'))

// 检查区块链数据
JSON.parse(localStorage.getItem('cosmos-blockchain'))

// 检查挖矿记录
JSON.parse(localStorage.getItem('mining-rewards'))
```

## ⚠️ 常见问题排除

### 问题1：余额没有更新
**解决方案**: 
- 等待区块确认（可能需要挖矿）
- 手动点击"刷新余额"
- 检查交易是否在交易池中

### 问题2：转账失败
**可能原因**:
- 余额不足
- 地址格式错误
- 网络连接问题

### 问题3：挖矿没有奖励
**检查要点**:
- 确认挖矿完成（进度条100%）
- 检查是否选择了正确的钱包
- 查看挖矿历史记录

### 问题4：区块浏览器显示异常
**解决方案**:
- 刷新页面重新加载
- 检查区块链是否正常运行
- 查看浏览器控制台错误

## 📊 性能指标

**正常性能指标**:
- 钱包创建: < 2秒
- 代币铸造: < 3秒  
- 代币转账: < 2秒
- 挖矿完成: 2-10秒（根据难度）
- 区块确认: 自动每30秒
- 页面响应: < 1秒

## 🎉 测试完成确认

所有功能测试通过后，您应该能看到：

1. **钱包管理** ✅
   - 多个钱包正常创建和管理
   - 余额实时同步更新

2. **代币生产** ✅
   - 成功铸造多种代币
   - 余额正确增加

3. **代币转账** ✅  
   - 转账成功执行
   - 发送方和接收方余额正确更新

4. **挖矿功能** ✅
   - 挖矿奖励正常发放
   - 统计数据准确

5. **区块浏览器** ✅
   - 实时显示最新区块
   - 历史区块查询正常

6. **数据持久化** ✅
   - 刷新页面后数据保持
   - 区块链状态持续

**🎊 恭喜！您已经拥有一个功能完整的JavaScript区块链应用！**